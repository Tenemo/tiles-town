{
    "name": "@tiles-town/contracts",
    "version": "1.0.0",
    "private": true,
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "clean": "node -e \"const fs=require('fs'); const path=require('path'); fs.rmSync('dist',{recursive:true,force:true}); for (const entry of fs.readdirSync('src')) { if (/\\.(js|js\\.map|d\\.ts|d\\.ts\\.map)$/.test(entry)) { fs.rmSync(path.join('src', entry), { force: true }); } }\"",
        "build": "pnpm run clean && tsc --project tsconfig.json",
        "dev": "tsc --project tsconfig.json --watch",
        "lint": "tsc --project tsconfig.json --noEmit",
        "typecheck": "tsc --project tsconfig.json --noEmit"
    },
    "dependencies": {
        "@sinclair/typebox": "^0.34.49"
    },
    "devDependencies": {
        "typescript": "^6.0.2"
    }
}
